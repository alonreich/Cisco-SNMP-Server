# Install ONLY via:  Install.bat  (uses .\venv — do not pip install into global Python)
flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.36.1
flask-cors>=4.0.0
# SNMP (one of — chosen by Python version)
pysnmp-lextudio>=6.0.0; python_version < "3.14"
pysnmp>=7.1.0; python_version >= "3.14"
