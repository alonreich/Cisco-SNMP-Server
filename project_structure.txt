================================================================================
  CISCO SNMP MASTER — THE DEFINITIVE TECHNICAL MANIFESTO (V4.0)
================================================================================
  Updated: Friday, June 12, 2026
  Status: Production-Grade NMS (ZERO-CACHE ARCHITECTURE)
  Primary Stack: Python 3.14, Flask, SocketIO, Eventlet, SQLite, vis.js, Tailwind

────────────────────────────────────────────────────────────────────────────────
  1. CORE ARCHITECTURAL PRINCIPLE: THE ZERO-CACHE MANDATE
────────────────────────────────────────────────────────────────────────────────
  This system is engineered for absolute real-time accuracy. Caching at any 
  layer (Filesystem, Python Bytecode, Memory, or Browser) is strictly forbidden.

  ENFORCEMENT MECHANISMS:
  • Bytecode Suppression: 'sys.dont_write_bytecode = True' and 
    'set PYTHONDONTWRITEBYTECODE=1' are mandatory across all layers.
  • Memory Integrity: Data MUST NOT be stored in global variables or local 
    dictionaries. Every broadcast/request MUST trigger a direct SQLite read.
  • Browser Cache Busting: 'Cache-Control: no-store' headers are injected 
    into every HTTP response via the '@app.after_request' hook.
  • Process Isolation: 'master.py' and 'main.py' run in completely detached 
    background scopes via VBScript wrappers to prevent console host binding.

────────────────────────────────────────────────────────────────────────────────
  2. DATA PROCESSING PIPELINE (SNMP ➔ DB ➔ WS ➔ UI)
────────────────────────────────────────────────────────────────────────────────
  A. THE ENGINE (master.py):
     • Technology: Asyncio + modern 'pysnmp' v7 (v3arch).
     • Interval: 30-second cycles.
     • Logic: Parallel polling of all 'monitored_switches' in config.json.
     • Key OIDs:
       - CPU Load: 1.3.6.1.4.1.9.9.109.1.1.1.1.8 (Cisco 5min Avg)
       - Interfaces: .1.3.6.1.2.1.2.2.1 (Standard MIB-II)
       - Throughput: 1.3.6.1.2.1.31.1.1.1.6 (HC In) / .10 (HC Out)
       - Topology: 1.0.8802.1.1.2.1.4.1.1.9 (LLDP) / 1.3.6.1.4.1.9.9.23.1.2.1.1.6 (CDP)
       - Memory: 1.3.6.1.4.1.9.9.48.1.1.1 (Cisco Memory Pool)

  B. THE PERSISTENCE (database/history_db.py):
     • Schema:
       - [devices]: IP, Hostname, sys_descr, Status, last_seen.
       - [interface_history]: in/out octets (raw 64-bit counters), speed, admin/oper.
       - [cpu_history] / [memory_history]: Time-series telemetry.
       - [topology]: neighbor relationships (local_port ➔ remote_id/port).
     • Maintenance: Automatic FIFO trimming (Size capped at 20MB via VACUUM).

  C. THE BROADCASTER (main.py):
     • Technology: Flask + Socket.IO (Eventlet async_mode).
     • Background Loop: Wakes up every 15s.
     • Metrics Calculation: Computes Delta bps: ((curr - prev) * 8) / (dt * 10^6).
     • Event: socketio.emit('device_update', { devices, leaderboard })

────────────────────────────────────────────────────────────────────────────────
  3. DASHBOARD UI/UX SPECIFICATION (THE PERFECT STATE)
────────────────────────────────────────────────────────────────────────────────
  The Dashboard Interface Leaderboard is a rigid, high-density 1px black grid.

  VISUAL STANDARDS:
  • Theme: Deep Slate 700 (#334155). Text: Slate 100 (#f1f5f9).
  • Grid: 'border-collapse: collapse;' with '1px solid black' on all cells.
  • Spacing: Global py-4 padding. Table headers are compressed (py-2).

  LEADERBOARD COLUMN ARCHITECTURE (STRICT):
  1. [DEVICE] (110px-150px): 
     - Data: Management IP (bold, text-sm) + (TYPE) in 8px grey font.
     - Action: Clicking triggers an ALPHABETICAL drill-down of all interfaces.
  2. [INTERFACE] (180px fixed): 
     - Names: Shortened shorthand (Gi, Fa, Te, Po, Vl, Lo, Tu).
     - Alignment: Perfectly centered and middle-aligned (vertical/horizontal).
     - Wrapping: Max 3 lines before truncation.
  3. [DESCRIPTION] (120px-300px): 
     - Natural expansion up to 300px before line wrap.
  4. [SPEED] (100px fixed): 
     - Format: '100mb', '1gb', '10gb'. Red if descr/speed mismatch found.
  5. [TRAFFIC (mbps)] (100px fixed): 
     - Header: Lowercase 'mbps'.
     - Markers: Green (↓In) and Blue (↑Out). Shifted -12px left for balance.
  6. [UTILIZATION] (16rem min): 
     - Triple-threshold color bars (Green <30%, Yellow <75%, Red >75%).

────────────────────────────────────────────────────────────────────────────────
  4. OPERATIONAL INFRASTRUCTURE
────────────────────────────────────────────────────────────────────────────────
  • LAUNCHERS: 
    - main_background.vbs / master_background.vbs: Hidden window (Style 0).
  • RESTART PROTOCOL (restart_server.bat):
    1. Force-kill all 'python.exe' via PowerShell (Stop-Process -Force).
    2. Wait 2s for OS to release file locks on logs.
    3. Clear/Delete stale logs in /logs folder.
    4. Spawn detached VBS launchers.
  • DEPENDENCY NOTES:
    - VMware Targets: Require SNMP manual enablement via VAMI or CLI.
    - Python 3.14: Causes Eventlet RLock warnings; suppressed via stderr redirection.

────────────────────────────────────────────────────────────────────────────────
  END OF DOCUMENT — THE ULTIMATIVE SOURCE OF TRUTH
────────────────────────────────────────────────────────────────────────────────
